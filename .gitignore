# このファイルは Git に保存しないファイルやフォルダを決めるファイルです。
# 仮想環境。インストールした依存関係の実体なので、リポジトリには含めない。
.venv/

# Python が実行時に自動生成するキャッシュ。毎回作り直せるため管理不要。
__pycache__/
*.pyc
*.pyo
*.pyd

# テスト・型チェックの作業用キャッシュ。結果の再現には不要。
.pytest_cache/
.mypy_cache/

# ローカル実行で増える設定やデータ。
instance/
.env
todo_app.db
*.db
