<!-- このファイルは、これから追加したい機能や改善案をまとめたメモです。 -->
<!-- ROADMAP.md: まだ未実装だが、今後どこを広げたいかを残すメモ。優先順位や方向性の確認用。 -->
# ROADMAP

このファイルは今後の改善計画と運用メモをまとめたものです。

<!-- ここでは「確定した約束」ではなく、学習や発展の候補を自由に残している。 -->
<!-- 実装が簡単な順に並べてある。レベル1が一番軽く、レベル6が一番重い。 -->
## 後で追加したい機能（候補）

### レベル1：超かんたん（数十行〜150行）
- テーマ(壁紙の色)を青・緑・オレンジ・ピンク・水色など追加 — CSS変数を切り替えるだけ
- タスク削除時、「本当に消しますか？」と確認する2段階構成にする — モーダル1個追加
- ダークモード — CSS切替＋ローカル保存
- TODOだけ・DOINGだけ・DONEだけ・WISHだけの画面に切り替えられるようにする — URLパラメータでフィルタするだけ

### レベル2：かんたん（150〜400行）
- ショートカット（「＋」固定ボタン、クイック追加、テンプレ） — FAB（画面右下の浮かぶボタン）＋簡単なJS
- フィルタ強化（期限が近い順 / プロジェクト別 / 自分の担当だけ / タグ別 /など） — クエリでORDER BYとWHEREを変えるだけ
- プロジェクトの欄で、プロジェクトをクリックしたときにそのプロジェクトのタスクが表示されて、かつタスクをタップすると詳細画面に飛ぶようにする — ルート1〜2個追加
- ドラッグ＆ドロップでカンバン移動（TODO → DOING → DONE を指で移動） — SortableJSというライブラリを使えばかなり軽い
- マイページ作る（完了したタスクの数・未完了のタスクの数・今後7日間のタスク） — 集計クエリ＋テンプレ
- カレンダーページを作る。(カレンダーの日付をタップすると、その日締め切りのタスクが表示される) — 月のグリッドを描く＋日付クリック処理
- タグ / 優先度 — モデルにカラム追加＋フォーム＋表示
- レスポンシブ微調整 — メディアクエリでスマホ/タブレット/PCの崩れを直す。1箇所は軽いが全画面見直すと意外と時間がかかる

### レベル3：ふつう（400〜700行）
- 見積もり時間 / 実績時間の記録 — フィールド追加＋簡易タイマー
- 複数選択の一括操作（移動 / 削除 / プロジェクト変更 / 期限変更） — チェックボックスUI＋一括API
- ゴミ箱 / アーカイブ / 復元 — 論理削除（deleted_at列）＋復元画面
- 繰り返しタスク（毎週/毎日、スヌーズ） — 次回日付の計算ロジックが少しややこしい
- タスク依存関係（blocked by / blocks） — 中間テーブル＋表示
- コメント / 履歴（監査ログ）（だれがいつ完了にしたか） — モデル追加＋記録の差し込み
- インポート / エクスポート（CSV / JSON / Markdown） — フォーマットごとに変換コード
- アクセシビリティ（フォーカス / キーボード操作） — 1か所は小さいが、ページ全体に散らばる作業

### レベル4：やや難（700〜1,200行）
- 権限ロール（チームのOwner/Admin/Member、プロジェクトごと権限） — 全エンドポイントで権限チェックを入れる必要がある
- 招待リンク / 招待メールでチーム参加 — トークン発行＋メール送信設定
- Googleログイン対応（OAuthログイン） — Authlib等のライブラリで意外と短いが、認証フロー全体の理解が必要
- 添付ファイル・画像アップロード — ストレージ（ローカルorクラウド）の設計が必要
- 分析ダッシュボード（完了率 / 期限超過率 / リードタイム / プロジェクト別進捗） — 集計SQL＋Chart.jsなどのグラフ

### レベル5：難しい（1,200〜2,500行）
- REST API + OpenAPI/Swagger + Token認証 — 既存ルートをAPIに展開＋ドキュメント自動生成
- 通知（締切○日前に通知：PWAのPush通知 or メール通知） — VAPIDキー、Service Worker、Push購読の理解が必須
- バックグラウンドジョブ＋キュー（通知/集計/重い処理を分離） — Celery/RQ＋Redisのインフラ構築も含む
- 観測性（Observability：ログ/メトリクス/トレース） — Sentry、Prometheus、OpenTelemetry等の外部スタック連携

### レベル6：超難しい（2,000行〜）
- RPG化（クエスト化 + レベル制 + 実績） — 仕様次第。最小なら軽いが、本格化すると一気に膨らむ
- リアルタイム共同編集（WebSocket） — Flask-SocketIO＋同時編集のマージ
- オフライン・ファースト＋同期（衝突解決） — クライアント側DB＋同期キュー＋衝突解決ロジック、最重量
