# このファイルは、開発やテストのときだけ使う追加ライブラリの一覧です。
# 開発・検証専用の依存関係。
# 本番デプロイに混ぜないことで、必要なものだけを軽く入れられる。
# 将来 linter や formatter を増やす場合もここへ足していく。
pytest==8.3.5

# render.yaml の構文チェック用。デプロイ設定が壊れていないかをローカルで確かめるために使う。
# 本番では読み込まないため requirements.txt には入れない。
PyYAML==6.0.3
